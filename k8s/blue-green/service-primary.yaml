apiVersion: v1
kind: Service
metadata:
  name: petclinic-svc
  namespace: test-qa
  annotations:
    harness.io/primary-service: "true"
spec:
  selector:
    app: petclinic
  ports:
    - port: 80
      targetPort: 80
